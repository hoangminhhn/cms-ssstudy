"use client";

import React from "react";
import NotFound from "@/pages/NotFound";

/**
 * ModerationTab placeholder -> shows 404 until detailed requirements are provided.
 */
const ModerationTab: React.FC = () => {
  return <NotFound />;
};

export default ModerationTab;