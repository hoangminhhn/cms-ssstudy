"use client";

import React from "react";

/**
 * ReportsTab
 * Minimal placeholder: all previous demo features removed so the page is a clean slate
 * for implementing new report functionality.
 */

const ReportsTab: React.FC = () => {
  return (
    <div className="p-6">
      <div className="rounded-md border p-6 bg-white dark:bg-gray-800">
        <h2 className="text-lg font-semibold mb-2">Báo cáo xem chung</h2>
        <p className="text-sm text-muted-foreground">
          Trang đã được dọn sạch và hiện trống để bạn triển khai chức năng báo cáo mới.
        </p>
      </div>
    </div>
  );
};

export default ReportsTab;